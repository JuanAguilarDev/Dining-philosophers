package clases;
/* Un enum es un tipo de clase especial que limita la creacion de objetos a los especificados
explicitamente en la implementacion de la clase. Estos estados solo definen la posicion del palillo que
fue tomado.  */
public enum Estado {
    IZQUIERDA, DERECHA; 
}
